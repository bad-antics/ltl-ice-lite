// examples/demo.ltl — runnable demo for ltl_ice_lite

import std::io
use ltl_ice_lite as lib

fn main() {
    io::println("ltl_ice_lite demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}